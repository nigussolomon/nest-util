# nest-audit

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nest-audit` to build the library.

## Running unit tests

Run `nx test nest-audit` to execute the unit tests via [Jest](https://jestjs.io).
