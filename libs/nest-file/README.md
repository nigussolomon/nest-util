# nest-file

NestJS file storage package for storing encrypted files in MinIO and attaching those files to arbitrary TypeORM entities.

## Build

Run `nx build nest-file` to build the library.

## Unit tests

Run `nx test nest-file` to execute the unit tests.
