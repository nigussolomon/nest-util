export const NOTIFY_OPTIONS = 'NOTIFY_OPTIONS';

export const AUTH_PERMISSIONS_METADATA_KEY = 'auth:permissions';

/** Provider token for the real-time notifications gateway */
export const NOTIFY_GATEWAY = 'NOTIFY_GATEWAY';

/** Event emitted to a user's socket room when a new notification is persisted */
export const NOTIFICATION_CREATED_EVENT = 'notification:created';
